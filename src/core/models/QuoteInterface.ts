export interface Quote {
  anouncement: string;
  text: string;
  author: string;
  image: string;
  date: Date
}

export interface QuoteInput {
  anouncement: string;
  text: string;
  author: string;
  image: string;
}
